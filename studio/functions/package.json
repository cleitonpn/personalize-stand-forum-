{
  "name": "uset-stand-studio-functions",
  "private": true,
  "description": "Operações que exigem o Admin SDK — excluir login de expositor e definir senha provisória.",
  "main": "index.js",
  "engines": { "node": "22" },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.1.0"
  }
}
